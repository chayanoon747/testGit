import { View } from "react-native"

export const newScreen = ()=>{
    const feature = "yes"

    return(
        <View style={{flex:1}}>
            <Text>รักเบบี๋ที่สุดใน</Text>
        </View>
    )
}