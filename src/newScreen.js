import { View } from "react-native"

export const newScreen = ()=>{
    const feature = 0
    const featureAdd = 0
    const featureAdd2 = 0

    return(
        <View style={{flex:1}}>
            <Text>รักเบบี๋ที่สุดใน</Text>
        </View>
    )
}