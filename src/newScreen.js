import { View } from "react-native"

export const newScreen = ()=>{
    
    const featureAdd = 0
    const featureAdd2 = 0

    return(
        <View style={{flex:1}}>
            <Text>test feature  </Text>
        </View>
    )
}